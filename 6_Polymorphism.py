# ✅ **ШАГ 6: Полиморфизм (разные поведения при одинаковом интерфейсе)**

# **Задача 6: "Фигуры"**  
# Создай классы `Rectangle` и `Circle`, у каждого будет метод `area()`:
# - у прямоугольника площадь = длина × ширина
# - у круга площадь = π × радиус²

# **Зачем:** Чтобы научиться писать код, где **разные объекты могут вести себя одинаково** снаружи.

# ---

import math
class Rectangle: #прямоугольник
    def __init__(self, length: int, width: int):
        self.length = length
        self.width = width
        
    def area(self):
        return self.length * self.width
    
class Circle: #круг
    def __init__(self, radius: int):
        self.radius = radius
        
    def area(self):
        return math.pi * self.radius ** 2
    
    
rect = Rectangle(5, 3)
circle = Circle(3)
print(f"Ploshad Rectangle {rect.area()} metr")
print(f"Ploshad Circle {circle.area()} metr")

# ✨ Что ты тут изучаешь:
# Одинаковое поведение (area()) для разных объектов (Rectangle, Circle).

# Даже не зная, кто именно перед нами — Rectangle или Circle — можно просто вызвать area() и получить правильный результат.
# ➔ Это и есть полиморфизм ("много форм, но общий интерфейс").


# ❓ Мини-вопросы для закрепления:
# Почему методам нужен self?
# Что будет, если в обоих классах сделать одинаковое имя метода (area)?

# Ответ:
#     Отвечаю как наставник:

# ---

# ### ❓ Почему методам нужен `self`?

# Методам нужен `self`, потому что через него метод получает доступ к **конкретному объекту** (экземпляру класса), на котором он работает.  
# **`self` — это ссылка на самого себя**, на конкретный экземпляр.

# > Без `self` метод не будет знать, к каким именно данным (атрибутам) объекта он должен обращаться.

# **Пример:**  
# Когда ты пишешь `self.length`, Python понимает: "Ага, нужно взять длину именно вот у этого прямоугольника, а не у какого-то другого".

# ---

# ### ❓ Что будет, если в обоих классах сделать одинаковое имя метода (`area`)?

# **Это нормально и правильно!** Именно это и называется **полиморфизмом** (разное поведение при одинаковом названии методов).

# ✅ Когда ты вызовешь `.area()`, Python автоматически поймёт, **у какого объекта** ты вызываешь метод и **какую реализацию** метода нужно использовать.

# **Пример:**

# ```python
# r = Rectangle(5, 10)
# c = Circle(3)

# print(r.area())  # вызовет area() для прямоугольника
# print(c.area())  # вызовет area() для круга
# ```

# То есть **одно имя** — **разные результаты** в зависимости от объекта. Это и есть **сила ООП**! 🚀

# ---
